# react-scripts@5 still declares a TypeScript 3/4 peer range, but this project
# runs TypeScript 5 (and React 19), so installs need legacy peer resolution.
# Enabling it here means `npm install` works without the --legacy-peer-deps flag.
legacy-peer-deps=true
